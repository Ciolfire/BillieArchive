{% trans_default_domain domain|default('app') %}

<li class="nav-item col text-center">
  <a id="{{ name }}Tab" type="button" class="nav-link h-100 user-select-none" data-tabs-target="tab" data-action="click->tabs#show" data-tabs-target-param="{{ name }}">
    <span class="pe-none text-strong">
      {% include "_icon.html.twig" with {icon: icon|default(name)} only %}
      <span class="d-none d-lg-inline">{{ label|default(name) }}</span>
    </span>
  </a>
</li>