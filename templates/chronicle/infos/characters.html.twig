{% trans_default_domain 'app' %}

  <h2>{% include "_icon.html.twig" with { icon: 'characters', class: 'icon-lg' } only %}{{ "label.multi"|trans({}, 'character') }}</h2>
  <div class="row">
    {% if character.peekingRights|default(null) is empty %}
      {{ 'empty'|trans }}
    {% else %}
      {% for peeking in character.peekingRights %}
        {% if peeking.rights is not empty %}
          {% include 'character/_peeking_card.html.twig' with { character: peeking.target, access: peeking } %}
        {% endif %}
      {% endfor %}
    {% endif %}
  </div>
</div>
