{% extends 'base.html.twig' %}

{% trans_default_domain 'character' %}

{% block title %}{{ "characters"|trans }}{% endblock %}

{% block body %}

{% if characters is defined %}
  {% include "character/list.html.twig" with {'title': 'characters.label', 'characters': characters, 'type': 'character'} %}
{% endif %}
{% if npc is defined %}
  {% include "character/list.html.twig" with {'title': 'characters.npc', 'characters': npc, 'type': 'npc'} %}
{% endif %}
{% if generated is defined %}
  {% include "character/list.html.twig" with {'title': 'characters.generated', 'characters': generated, 'type': 'template'} %}
{% endif %}
{% endblock %}


{% block name %}

<div class="row">
{# <div class="row block block-{{ character.type }}" role="button" onclick='location.href="{{ path('character_show', {'id': character.id}) }}"'> #}

<div class="col"></div>
{% if type == "human" %}<div class="col">{{ character.type }}</div>{% endif %}
{# <div class="col">{{ character.player }}</div> #}
{# <div class="col">{{ character.concept }}</div> #}
{# <div class="col">{{ character.chronicle }}</div> #}
{# </div> #}
{# {% else %} #}
<td colspan="11">no records found</td>
</div>
</div>
</div>

{% endblock %}