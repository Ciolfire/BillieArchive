{% trans_default_domain 'character' %}

<div class="col-2">
    <a class="vampire-btn btn btn-sm btn-menu w-100" href="{{ path('character_embrace', {'id': character.id}) }}">
      {% include "icon.html.twig" with {'icon': 'vampire', 'hasTips': 0} only %}
      <span class="d-none d-md-inline">{{ "embrace.name"|trans }}</span>
    </a>
</div>
<div class="col-2">
  <a class="mage-btn btn btn-sm btn-menu w-100" href="#">
    {% include "icon.html.twig" with {'icon': 'mage', 'hasTips': 0} only %}
    <span class="d-none d-md-inline">{{ "awaken.name"|trans }}</span>
  </a>
</div>
<div class="col-2">
  <a class="werewolf-btn btn btn-sm btn-menu w-100" href="#">
    {% include "icon.html.twig" with {'icon': 'werewolf', 'hasTips': 0} only %}
    <span class="d-none d-md-inline">{{ "change.name"|trans }}</span>
  </a>
</div>