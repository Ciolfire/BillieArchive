{% extends 'base.html.twig' %}

{% trans_default_domain 'auspice' %}

{% set setting = 'werewolf' %}

{% block title %}
  {{ auspice.name }}
{% endblock %}

{% block body %}
  <div class="container block py-5">
    {% if is_granted('ROLE_ST') %}
      <a class="float-end" href="{{ path('werewolf_auspice_edit', {id: auspice.id}) }}">{% include "_icon.html.twig" with {icon: 'edit'} only %}</a>
    {% endif %}
  {% include "werewolf/auspice/_show.html.twig" with {isModal: 0} %}
  </div>
{% endblock %}

