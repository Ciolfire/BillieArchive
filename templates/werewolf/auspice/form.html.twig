{% extends 'werewolf/form.html.twig' %}

{% trans_default_domain 'auspice' %}

{% if form.vars.data.id|default(null) != null %}
  {% set action = 'edit' %}
{% else %}
  {% set action = 'new' %}
{% endif %}


{% block title %}
  {{ action|trans }}
{% endblock %}
{% block header %}
  {{ action|trans }}{% include "_icon.html.twig" with {icon : 'type/werewolf', class: 'icon-lg'} only %}
{% endblock %}

{% block form %}
  {% set auspice = form.vars.data %}
  {% if auspice.id %}
    <div class="row">
      <div class="col row">
        <div class="col-12">
          {% if auspice.emblem is not null %}
            <img class="logo" src="{{ asset(auspice.emblem, 'auspices_emblems') }}" alt="{{ auspice.name}}" height="200"/>
          {% endif %}
        </div>
        <div class="col-12">
          <h5>{{ "emblem"|trans(domain: "app") }}</h5>
        </div>
      </div>
    </div>
  {% endif %}
  {{ parent() }}
{% endblock %}
