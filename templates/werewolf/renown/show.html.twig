{% extends 'base.html.twig' %}

{% trans_default_domain 'renown' %}

{% set setting = 'werewolf' %}

{% block title %}
  {{ renown.name }}
{% endblock %}

{% block body %}
  <div class="container block py-5">
    {% if is_granted('ROLE_ST') %}
      <a class="float-end" href="{{ path('werewolf_renown_edit', {id: renown.id}) }}">{% include "_icon.html.twig" with {icon: 'edit'} only %}</a>
    {% endif %}
  {% include "werewolf/renown/_show.html.twig" with {isModal: 0} %}
  </div>
{% endblock %}

