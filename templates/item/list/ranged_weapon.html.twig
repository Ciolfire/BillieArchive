{% extends "item/info-table.html.twig" %}

{% set cols = {
  'damage': 'damage',
  'ranges': 'ranges',
  'clip': 'clip',
  'strength': 'strength',
}|merge(cols) %}

{# {% set special = {
}|merge(special) %} #}
