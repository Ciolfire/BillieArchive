<div class="col-12">
  {% if character.clan.parentClan %}
    {{ character.clan.parentClan.name }} ({{ character.clan.name }})
  {% else %}
    {{ character.clan.name }}
  {% endif %}
</div>
<div class="col-12">
  {{"Apparent Age:"|trans }} {{ character.apparentAge }}
</div>
