{% trans_default_domain 'app' %}


{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary w-25">
  {% include "_icon.html.twig" with {'icon' : action, 'hasTips': 0} only %}
  {{ ("action." ~ button_label|default('action.save'))|trans }}
</button>
{{ form_end(form) }}
