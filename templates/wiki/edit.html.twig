{% extends 'base.html.twig' %}

{% trans_default_domain 'app' %}

{% block title %}
  {{  "action.#{action}"|trans }}
{% endblock %}

{% block body %}
  <div class="block block-neutral container">
    <h1 class="py-3">
      {% include "_icon.html.twig" with {icon : entity, class: 'icon-lg' } only %}
      {{  "#{form.vars.data.name} — " ~ "action.edit"|trans }}
    </h1>

    <div class="row block">
      <div class="col p-3">
        {{ include('_form.html.twig', {'button_label': action|default(null)}) }}
        {{ include('_back.html.twig') }}
      </div>
    </div>
  </div>
{% endblock %}
