{% trans_default_domain 'character' %}

{% if app.user == character.player or is_granted('ROLE_ST') %}
  <div class="col-auto">
      <a class="vampire btn btn-sm btn-menu w-100 text-truncate" href="{{ path('character_embrace', {'id': character.id}) }}">
        {% include "_icon.html.twig" with {icon: 'type/vampire'} only %}
        <span class="d-none d-md-inline">{{ "embrace.label"|trans([], 'vampire') }}</span>
      </a>
  </div>
  <div class="col-auto">
    <a class="mage btn btn-sm btn-menu w-100 text-truncate" href="#">
      {% include "_icon.html.twig" with {icon: 'type/mage'} only %}
      <span class="d-none d-md-inline">{{ "awaken.label"|trans([], 'mage') }}</span>
    </a>
  </div>
  <div class="col-auto">
    <a class="werewolf btn btn-sm btn-menu w-100 text-truncate" href="#">
      {% include "_icon.html.twig" with {icon: 'type/werewolf'} only %}
      <span class="d-none d-md-inline">{{ "change.label"|trans([], 'werewolf') }}</span>
    </a>
  </div>
  <div class="col-auto">
    <a class="btn-primary btn btn-sm btn-menu w-100 text-truncate" href="{{ path('character_lesser_add', {'id': character.id}) }}">
      {% include "_icon.html.twig" with {icon: 'lesser-template'} only %}
      <span class="d-none d-md-inline">{{ "template.lesser.label"|trans }}</span>
    </a>
  </div>
{% endif %}