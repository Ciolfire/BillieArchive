{% extends 'base.html.twig' %}

{% trans_default_domain 'character' %}

{% block body %}
	{% include "_back.html.twig" with {link: path('character_show', {id: character.id}) } %}
	<div class="container-fluid container-lg py-3" data-controller="character--access">
		<div class="row justify-content-center">
			<h1 class="text-center mt-5">{{ ('infos.details.edit')|trans }}</h1>
        <div class="col-auto block p-5 flex-column">
          <button name="heard" class="btn btn-primary d-block w-100 my-3" type="button" data-character--access-target="btn" data-action="click->character--access#update">Heard of</button>
          <button name="seen" class="btn btn-primary d-block w-100 my-3" type="button" data-character--access-target="btn" data-action="click->character--access#update">Seen once</button>
          <button name="acquaintance" class="btn btn-primary d-block w-100 my-3" type="button" data-character--access-target="btn" data-action="click->character--access#update">Acquaintance</button>
          <button name="know" class="btn btn-primary d-block w-100 my-3" type="button" data-character--access-target="btn" data-action="click->character--access#update">Know well</button>
          <button name="all" class="btn btn-primary d-block w-100 my-3" type="button" data-character--access-target="btn" data-action="click->character--access#update">Know intimately</button>
        </div>
        <div class="col block p-5">
				{{ form_start(form) }}
        {{ form_row(form.rights)}}
        {{ form_rest(form) }}
      <div class="text-center">
        <button class="btn btn-primary w-100">
          {% include "_icon.html.twig" with {icon: 'save'} %}
          {{ ('action.save')|trans({}, 'app') }}
        </button>
      </div>
  {{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}
