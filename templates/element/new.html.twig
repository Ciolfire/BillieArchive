{% extends 'form.html.twig' %}

{% trans_default_domain 'app' %}

{% if entity.id is null %}
  {% set action = "new" %}
{% else %}
  {% set action = "edit" %}
{% endif %}

{% block title %}
  {{ ("label.single")|trans([], element) }}
  —
  {{ ("action." ~ action)|trans }}
{% endblock %}
